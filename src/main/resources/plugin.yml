name: NovaCutscenes
version: '${version}'
main: xyz.novaserver.cutscenes.Cutscenes
api-version: 1.18
authors: [Nova]
depend: [ProtocolLib]
softdepend: [Themis, floodgate, TAB, NovaPlaceholders]
commands:
  cutscene:
    description: Manages cutscenes.
    permission: cutscenes.command
permissions:
  cutscenes.command:
    default: op
    description: Gives persmission to /cutscene command